---
"@fly4ai/sandcastle": minor
---

Add opt-in host verification after execution and evidence export, before merging a candidate. Verification uses a bounded argv command with versioned JSON context, accepts or retains a candidate, preserves recovery state on failure, and fast-forwards the exact checked commit.
