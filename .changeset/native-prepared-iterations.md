---
"@fly4ai/sandcastle": minor
---

Add native host preparation and per-iteration structured output before verified merge. Preserve opaque task metadata, actual source identity, raw/validated results and recovery references across iterations; distinguish no-work, blocked, retained and iteration-limit outcomes. Keep legacy single-run Output semantics separate from guarded queue execution.
