# FLY4AI release preparation

Readiness: ready for code preparation and Git delivery. npm publication is explicitly owned by the user and is not an acceptance requirement for this task.

Contract: the user requested merging the accepted AFK work to both remote main branches, refreshing local main and running skills/link-skills.sh, and preparing Sandcastle under the `fly4ai` organization scope. The follow-up delegates publication to the user. Starting points: Sandcastle `ddc0286`, skills `30d9554`; remote main revisions are `e99f832` and `0ebb852` respectively.

| Criterion | Required expected result                                                                                                                                                   | Proof                                                                                                                          |
| --------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------ |
| R1        | Package identity is `@fly4ai/sandcastle`, repository metadata points at the fork, and the next minor version is 0.13.0 with the existing changesets included               | Changesets version output, package/lockfile/changelog assertions; npm pack manifest and installed public import                |
| R2        | Current init templates, executable examples and installation docs use the new scope; existing runtime behavior remains passing                                             | Updated existing InitService assertions, complete typecheck/build/test gates; installed tarball CLI/init smoke check           |
| R3        | Skills describe the maintained fork and still enforce protocol capability, with no project-specific runner; Kimi's existing peer dependency has a usable installation path | Skills integrity; offline installed package/adapter construction with a local alias of the same fork package, no model request |
| R4        | Both remote main branches contain the final changes; skills local main is current and skill links resolve to it; Sandcastle local main is current                          | Non-forced Git push and remote/local revision equality, clean status, link target checks                                       |
| R5        | Publication remains under user control                                                                                                                                     | Release workflow requires manual dispatch; no npm publish, release tag or registry mutation is executed                        |

Required gates: Sandcastle `npm run typecheck`, `npm run build`, `npm test` in that order; skills `bash .githooks/pre-commit`; final `git diff --check` in both repositories. Existing P8 proves actual AFK model execution and the installed shared workflow behavior; this package rename uses deterministic package/init/import checks instead of another paid model trial. Baseline comparison is not required; named existing regression assertions remain binding.

Tools are available from P8. Use Node 24.21.0 and installed npm/Changesets, plus temporary npm cache and isolated install directories. Keep prior P8 package/session/evidence bytes and reports unchanged. Raw evidence belongs in `acceptance/runs/FLY4AI-RELEASE/`; commit the final report and preparation changes. Independently review the release diff against this plan before final acceptance and merging.
