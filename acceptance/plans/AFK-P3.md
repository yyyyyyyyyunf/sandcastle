# AFK-P3 — Durable evidence and cumulative recovery

Readiness: ready. Starting implementation revision: `c4867c2`. P0 prerequisites and P1/P2 execution termination/activity checks have passed. Contract: P3 in the approved skills implementation plan; seams and gates: `docs/agents/acceptance.md`.

| Criterion | Execution and expected proof                                                                                                                                                                                                                                                        |
| --------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| AC-P3.1   | `src/run-recovery.test.ts`: real temporary Git plus scripted native agent, first iteration dirty then clean or failed. Public results/errors retain every preserved worktree reference; the legacy single path identifies the most recent preserved worktree.                       |
| AC-P3.2   | `src/run-artifacts.test.ts`: declare gitignored evidence directories, produce different content over multiple attempts, then read host snapshots after removable worktrees are actually gone. Each attempt has its own directory.                                                   |
| AC-P3.3   | Public artifact configuration and controlled filesystem failures: invalid source/root mapping, existing destination content and copy/record errors preserve recoverable work and stop progression. Git cleanliness alone never permits deleting the only copy of declared evidence. |
| AC-P3.4   | Read the host JSON run record after success and failure; verify completed iterations, candidate/merged revisions, errors, cleanup state and recovery references. Fail record persistence and assert no further agent allocation.                                                    |

Implementation direction: an opt-in declaration of host artifact root and repository-relative evidence directories. Use native/shared host filesystem export for noSandbox and bind mounts. Reject unsupported isolated export before agent execution. Snapshots use unique run/attempt paths and cannot replace user content; the agent writes ordinary repository-relative evidence paths. Run records are persisted before cleanup can remove the source and finalized before another iteration. Post-cleanup recording failure must still expose the already persisted candidate/artifact recovery references and stop.

Use existing public run/worktree/provider fixtures and Node filesystem operations; no tracker or model calls are needed. Provider mocks may inject transport failures but do not claim live container coverage. Cases are introduced in red/green slices, with logs under `acceptance/runs/AFK-P3/`. New behavior uses expected outcomes, not a comparative product baseline. Independent two-axis review precedes acceptance; required gates are typecheck → build → full tests.
